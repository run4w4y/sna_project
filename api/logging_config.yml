logger:
  path: /var/logs
  filename: sna_project.log
  level: debug
  rotation: 10 days
  retention: 1 months
  format: "<level>{level: <8}</level> | <green>{time:YYYY-MM-DD HH:mm:ss.SSS}</green> - <cyan>{name}</cyan> - <level>{message}</level>"
